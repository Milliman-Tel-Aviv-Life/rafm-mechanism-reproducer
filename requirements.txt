# Used by Streamlit Community Cloud (it prefers this file over pyproject.toml).
# Keep in sync with [project.dependencies] in pyproject.toml.
streamlit>=1.58
openai>=2.0
anthropic>=1.3
instructor>=1.15
pydantic>=2.13
openpyxl>=3.1
python-dotenv>=1.0
pyyaml>=6.0
